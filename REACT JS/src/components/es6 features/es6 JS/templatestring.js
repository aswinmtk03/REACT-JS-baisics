const a = 5;
const b= 10;
console.log(`The sum of ${a} and ${b} is ${a+b}`);

const multiLine = `is is a 
multi-line string
using template literals.`;
console.log(multiLine);

const price=50;
const discount=10;
console.log(`Final price: $${price-discount}`);